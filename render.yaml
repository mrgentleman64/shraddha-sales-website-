services:
  - type: web_service
    name: shradhasales-backend
    env: python
    plan: starter
    branch: main
    root: backend
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn server:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: JWT_SECRET
        sync: false
      - key: MONGO_URL
        sync: false
      - key: DB_NAME
        value: shradhasales
      - key: ADMIN_EMAIL
        value: admin@shradhasales.com
      - key: ADMIN_PASSWORD
        value: admin123
  - type: static_site
    name: shradhasales-frontend
    branch: main
    root: frontend
    buildCommand: npm install && npm run build
    publishPath: dist
    envVars:
      - key: NODE_ENV
        value: production
